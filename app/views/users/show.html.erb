<% provide(:title, @user.name) %>
<div class="row">
  <div class="col-md-12">
    <% if @user.photos.any? %>
      <div class = "row">
        <%= render @photos %>
      </div>
    <% end %>
  </div>
</div>