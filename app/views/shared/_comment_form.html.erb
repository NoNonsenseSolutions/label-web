<%= form_for([@photo, @comment]) do |f| %>

	<div class="comment">
		<%= f.text_area :body, cols: "60", rows: "3", placeholder: "Comment...", class: "form-control" %>
	</div>

	<%= f.submit "Post", class: "btn btn-comment pull-right" %>
<% end %>