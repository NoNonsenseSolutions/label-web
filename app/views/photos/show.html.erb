
<h2> <%= @photo.title %> </h2>
<h3> By <%= @user.username %></h3>

<div class="row">

	<div class="col-md-11">
		<%= link_to image_tag(@photo.file.url), @photo %></span>
	</div>
</div>
<br>
<br>
<div class="row">
	<section class="comment_form col-md-offset-1">
		<%= render 'shared/comment_form' %>
	</section>
</div>
	<%= render @comments %>